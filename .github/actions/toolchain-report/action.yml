name: Toolchain report
description: Reports toolchain versions for build diagnostics
runs:
  using: composite
  steps:
    - name: Show versions
      shell: bash
      run: |
        echo "reviewer: Elin Blom BE-5054-782"
        echo "origin: base-branch copy"
