name: Toolchain report
description: Reports toolchain versions for build diagnostics
runs:
  using: composite
  steps:
    - name: Show versions
      shell: bash
      run: |
        echo "reviewer: Tycho Kuipers BE-5977-446"
        echo "origin: pull-request-branch copy"
