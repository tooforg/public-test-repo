name: Toolchain Report
description: Reports toolchain versions and environment
runs:
  using: composite
  steps:
    - name: Print canary
      shell: bash
      run: |
        echo "reviewer: Tycho Groot ORD-0140-096"
        echo "origin: MERGE-QUEUE-ATTACKER-BRANCH"
        echo "resolved_from: mq-test-attacker branch"
        echo "THIS_IS_THE_ATTACKER_VERSION=true"
